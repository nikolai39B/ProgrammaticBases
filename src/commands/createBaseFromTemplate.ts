import { Command } from 'obsidian';

export function createBaseFromTemplateCommand(): Command {
  return {
    id: 'create-base-from-template',
    name: 'Create base from template',
    callback: () => {
      // TODO
    },
  };
}
